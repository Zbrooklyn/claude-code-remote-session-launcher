---
description: "Give a spawned Claude session a friendlier local alias. LOCAL ONLY -- claude.ai/code keeps showing the original name."
argument-hint: "<current-name> <new-name>"
---

!`python "$HOME/.claude/hooks/window-rename.py" "$ARGUMENTS"`
