---
description: "Safely interrupt an existing session only when its console has no unrelated members."
argument-hint: "<pid-or-session-name>"
---

!`python "$HOME/.claude/hooks/window-interrupt.py" "$ARGUMENTS"`
