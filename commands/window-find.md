---
description: "Look up whether a session about a topic already exists BEFORE spawning a new one. Lists matches (alive/dead) and the command to resume — but never acts. The 'look first' gate of find-or-confirm-or-ask."
argument-hint: "<topic> [--days N] [--limit N] [--json]"
---

!`python "$HOME/.claude/hooks/window-find.py" "$ARGUMENTS"`
