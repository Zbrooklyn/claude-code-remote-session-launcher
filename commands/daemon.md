---
description: "Fresh headless background Claude Code session, reachable only via Anthropic remote control (claude.ai/code or app). Use --name <label> for a friendly session name."
argument-hint: "[workspace-path] [\"first prompt\"] [--worktree] [--name <label>]"
---

!`python "$HOME/.claude/hooks/spawn-window.py" daemon "$ARGUMENTS"`
