---
description: "Run the durable Windows-native worker orchestration controller."
argument-hint: "<spawn|task-create|depends|dispatch|handoff|verify|monitor|recover|close|workers|tasks|audit> ..."
---

!`python "$HOME/.claude/hooks/orchestrator.py" $ARGUMENTS`
