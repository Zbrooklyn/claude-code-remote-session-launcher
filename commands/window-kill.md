---
description: "Kill a spawned Claude session by its remote-control name. Use '--all' to kill every spawned session."
argument-hint: "<session-name> | --all"
---

!`python "$HOME/.claude/hooks/window-kill.py" "$ARGUMENTS"`
